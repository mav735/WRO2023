const float g_wheelDiameter = 62.25;                                                // mm
const float g_wheelBase = (180.05) / g_wheelDiameter / PI * 360;                       // mm -> degrees
const float g_distBetweenSensors = (105) / g_wheelDiameter / PI * 360;              // mm -> degrees
const float g_distBetweenSensorsAndWheelBase = 167;    // mm -> degrees
