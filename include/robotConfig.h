const float g_wheelDiameter = 62.25;                                                // mm
const float g_wheelBase = (119) / g_wheelDiameter / PI * 360;                       // mm -> degrees
const float g_distBetweenSensors = (105) / g_wheelDiameter / PI * 360;              // mm -> degrees
const float g_distBetweenSensorsAndWheelBase = 50 / g_wheelDiameter / PI * 360;    // mm -> degrees
